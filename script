local WindUI = loadstring(game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua"))()

local Window = WindUI:CreateWindow({
    Title = "LOL",
    Icon = "star",
    Theme = "Dark",
    Folder = "LOL",
})

local localplayer = Window:Tab({
    Title = "localplayer",
    Icon = "user",
})

local ESP = Window:Tab({
    Title = "ESP",
    Icon = "home",
})

localplayer:Slider({
  Flag = "WSslider",
  Title = "Walkspeed",
  Step = 1,
  Value = {
    Min = 0,
    Max = 50,
    Default = 0,
  },
  Callback = function(value)
    getgenv().WSslider = value
  end,
})

localplayer:Toggle({
  Flag = "WSEnableToggle",
  Title = "Enable",
  Value = false,
  Callback = function(value)
        if value then
            local hum = game.Players.LocalPlayer.Character.Humanoid
            while value do
                hum.WalkSpeed = 16 + getgenv().WSslider
                task.wait(0.1)
            end
        else
            game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = 16
        end
  end,
})

local EvidenceDescendantChecker
ESP:Toggle({
    Flag = "GhostOrbToggle",
    Title = "Ghost Orb ESP",
    Value = false,
    Callback = function(value)
        if value then
            local function addhighlight(Evidence)
                if not Evidence:FindFirstChildOfClass("Highlight") and not Evidence:FindFirstChildOfClass("BillboardGui") then
                    local billboard = Instance.new("BillboardGui")
                    local label = Instance.new("TextLabel")
                    local highlight = Instance.new("Highlight")

                    billboard.Adornee = Evidence -- Must attach to a part/model
                    billboard.Size = UDim2.new(4, 0, 2, 0) -- Width, height
                    billboard.MaxDistance = 10000 -- Visibility range
                    billboard.StudsOffset = Vector3.new(0, 1, 0) -- Offset from part
                    billboard.AlwaysOnTop = true

                    label.Size = UDim2.new(1, 0, 1, 0) -- Fill parent
                    label.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
                    label.BackgroundTransparency = 1
                    label.TextColor3 = Color3.fromRGB(255, 165, 0)
                    label.TextSize = 14
                    label.Text = Evidence.Name
                    label.FontFace = Font.new(
                        "rbxasset://fonts/families/LuckiestGuy.json",
                        Enum.FontWeight.Regular,
                        Enum.FontStyle.Normal
                    )

                    highlight.FillTransparency = 0.5
                    highlight.OutlineTransparency = 1
                    highlight.FillColor = Color3.fromRGB(255, 165, 0)
                    highlight.Adornee = Evidence

                    label.Parent = billboard
                    billboard.Parent = Evidence
                    highlight.Parent = Evidence
                    Evidence.Transparency = 0
                end
            end

            for _, Evidence in ipairs(game.Workspace:GetDescendants()) do
                if Evidence.Name == "GhostOrb" then
                    addhighlight(Evidence)
                end
            end
            EvidenceDescendantChecker = game.Workspace.DescendantAdded:Connect(function(Evidence)
                if Evidence.Name == "GhostOrb" then
                    addhighlight(Evidence)
                    print("bruh")
                end
            end)
        else
            for _, Evidence in ipairs(game.Workspace:GetDescendants()) do
                if Evidence:FindFirstChildOfClass("Highlight") and Evidence:FindFirstChildOfClass("BillboardGui") and Evidence.Name == "GhostOrb" then
                    Evidence:FindFirstChildOfClass("Highlight"):Destroy()
                    Evidence:FindFirstChildOfClass("BillboardGui"):Destroy()
                    Evidence.Transparency = 1
                    print("jewjej")
                end
            end
            EvidenceDescendantChecker:Disconnect()
            EvidenceDescendantChecker = nil
        end
    end,
})

local HandprintDescendantChecker
ESP:Toggle({
  Flag = "HandprintToggle",
  Title = "Handprint ESP",
  Desc = "Toggle Description",
  Value = false,
  Callback = function(value)
        if value then
            local function highlight(Handprint)
                if not Handprint:FindFirstChildOfClass("Highlight") and not Handprint:FindFirstChildOfClass("BillboardGui") then
                    local billboard = Instance.new("BillboardGui")
                    local label = Instance.new("TextLabel")
                    local highlight = Instance.new("Highlight")

                    billboard.Adornee = Handprint -- Must attach to a part/model
                    billboard.Size = UDim2.new(4, 0, 2, 0) -- Width, height
                    billboard.MaxDistance = 10000 -- Visibility range
                    billboard.StudsOffset = Vector3.new(0, 1, 0) -- Offset from part
                    billboard.AlwaysOnTop = true

                    label.Size = UDim2.new(1, 0, 1, 0) -- Fill parent
                    label.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
                    label.BackgroundTransparency = 1
                    label.TextColor3 = Color3.fromRGB(255, 165, 0)
                    label.TextSize = 14
                    label.Text = Handprint.Name
                    label.FontFace = Font.new(
                        "rbxasset://fonts/families/LuckiestGuy.json",
                        Enum.FontWeight.Regular,
                        Enum.FontStyle.Normal
                    )

                    highlight.FillTransparency = 0.5
                    highlight.OutlineTransparency = 1
                    highlight.FillColor = Color3.fromRGB(255, 165, 0)
                    highlight.Adornee = Handprint

                    label.Parent = billboard
                    billboard.Parent = Handprint
                    highlight.Parent = Handprint
                    Handprint.Transparency = 0
                end
            end

            for _, Handprint in ipairs(game.Workspace.Handprints:GetDescendants()) do
                if Handprint.Parent.Parent.Name == "Handprints" and Handprint:IsA("Decal") then
                    highlight(Handprint)
                end
            end
            HandprintDescendantChecker = game.Workspace.Handprints.DescendantAdded:Connect(function(Handprint)
                if Handprint.Parent.Parent.Name == "Handprints" and Handprint:IsA("Decal") then
                    highlight(Handprint)
                end
            end)
        else
            for _, Handprint in ipairs(game.Workspace.Handprints:GetDescendants()) do
                if Handprint.Parent.Parent.Name == "Handprints" and Handprint:IsA("Decal") and Handprint:FindFirstChildOfClass("BillboardGui") and Handprint:FindFirstChildOfClass("Highlight") then
                    Handprint:FindFirstChildOfClass("BillboardGui"):Destroy()
                    Handprint:FindFirstChildOfClass("Highlight"):Destroy()
                end
            end
            HandprintDescendantChecker:Disconnect()
            HandprintDescendantChecker = nil
        end
  end,
})

WindUI:Notify({
    Title = "iaubeiaubfe",
    Content = "bruh",
    Icon = "solar:bell-bold",
    Duration = 5,
})

WindUI:AddTheme({
    Name       = "Ocean",

    Accent     = Color3.fromHex("#0c3547"),
    Dialog     = Color3.fromHex("#071e2b"),
    Outline    = Color3.fromHex("#38bdf8"),
    Text       = Color3.fromHex("#e0f2fe"),
    Placeholder = Color3.fromHex("#4ea8c9"),
    Background = Color3.fromHex("#040f18"),
    Button     = Color3.fromHex("#0369a1"),
    Icon       = Color3.fromHex("#38bdf8"),
    Toggle     = Color3.fromHex("#0ea5e9"),
    Slider     = Color3.fromHex("#0ea5e9"),
    Checkbox   = Color3.fromHex("#0ea5e9"),

    PanelBackground            = Color3.fromHex("#FFFFFF"),
    PanelBackgroundTransparency = 0.95,
})

WindUI:SetTheme("Ocean")
